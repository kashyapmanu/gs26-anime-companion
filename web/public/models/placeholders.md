# Model placeholder / manual fetch

A sample model is already present at `web/public/models/sample.vrm`. If you prefer to
fetch a model manually (for example, to keep the repo small), delete the included
`sample.vrm` and place your own VRM file at this path, then update `CREDITS.md`.

See `README.md` for license guidance and suggested sources.
